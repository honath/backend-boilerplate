const asyncErrorBoundary = require("../errors/asyncErrorBoundary");
const service = require("./route.service");

/**
 * @async @function name
 * uses service.METHOD
 * @returns {res.status(STATUS).json({data: ...})}
 * Appropriate HTTP return status and
 * JSON formatted response data
 * @exports as name wrapped in asyncErrorBoundary
 */

module.exports = {};
